import subprocess

def create_wireguard_config(user_id):

    file_name = f"wg_{user_id}.conf"

    subprocess.run([
        "wg-gen-client",
        str(user_id)
    ])

    return open(file_name, "rb")
